A simple program that runs a command, pass the output to an ASCII art converter (and has its own too), and centers them in ncurses.

This program has been created to be used with potato-c. Can also used as a timer, or anything that you want!
